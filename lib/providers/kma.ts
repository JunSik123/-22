export async function getShortForecast(params: { lat: number; lon: number; date: string; }) {
  // 기상청 단기예보 API 호출 필요
  // api가 필요합니다
  return {};
}

export async function getMidForecast(params: { lat: number; lon: number; date: string; }) {
  // 기상청 중기예보 API 호출 필요
  // api가 필요합니다
  return {};
}
