export async function geocodeAddress(address: string) {
  // 카카오 로컬 API 주소 검색
  // api가 필요합니다
  return { lat: 0, lon: 0 };
}

export async function coord2region(lat: number, lon: number) {
  // 카카오 로컬 API 좌표 -> 행정구역 변환
  // api가 필요합니다
  return { region: '' };
}
